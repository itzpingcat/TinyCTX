llm:
  model: gpt-oss-20b-richard-erkhov
  base_url: http://100.64.72.3:5000
  api_key: dummy
  max_tokens: 4096
  temperature: 1
  timeout: 60

modules_path: ./modules
sessions_dir: ./sessions

# global agent limits
max_iterations: 20
token_budget: 16384

# per-extension config overrides
modules:
  ctx_tools:
    same_call_dedup_after: 3
    tool_trim_after: 10
    tool_output_truncate_after: 2
    max_tool_output_chars: 2000
  filesystem:
    uploads_dir: /mnt/uploads
    page_size: 2000
    cache_size: 128

disabled_modules: []