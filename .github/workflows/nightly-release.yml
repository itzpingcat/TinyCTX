# Nightly release: only cuts a release if both flake8 and pytest pass.
name: Nightly release
on:
  schedule:
    - cron: "0 7 * * *"   # 07:00 UTC daily
  workflow_dispatch: {}
permissions:
  contents: write
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python 3.14
      uses: actions/setup-python@v5
      with:
        python-version: "3.14"
        cache: "pip"
    - name: Install flake8
      run: |
        python -m pip install --upgrade pip
        pip install flake8
    - name: Lint with flake8
      run: |
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
        flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python 3.14
      uses: actions/setup-python@v5
      with:
        python-version: "3.14"
        cache: "pip"
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
    - name: Test with pytest
      run: |
        pytest -q

  release:
    needs: [lint, test]
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
    - name: Check for new commits since last nightly
      id: check
      run: |
        last_tag=$(git tag --list 'nightly-*' --sort=-creatordate | head -n1)
        if [ -z "$last_tag" ] || ! git diff --quiet "$last_tag" HEAD; then
          echo "skip=false" >> "$GITHUB_OUTPUT"
        else
          echo "skip=true" >> "$GITHUB_OUTPUT"
        fi
    - name: Tag and publish nightly release
      if: steps.check.outputs.skip != 'true'
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        tag="nightly-$(date -u +%Y%m%d)"
        git tag "$tag"
        git push origin "$tag"
        gh release create "$tag" \
          --title "$tag" \
          --notes "Automated nightly build. Passed flake8 and pytest on $(date -u +%Y-%m-%d)." \
          --prerelease \
          --target "${{ github.sha }}"
