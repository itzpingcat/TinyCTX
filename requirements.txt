# Install the package and its CLI-only dependencies from pyproject.toml.
# The full agent runtime (gateway, bridges, ai.py, etc.) normally runs inside
# Docker via the provided Dockerfile/compose.yaml. To run it on the host
# instead, install the `agent` extra:
#   .[agent]
-e .

# Dev / test tools:
pytest
pytest-asyncio
