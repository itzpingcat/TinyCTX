# Install the package and all its dependencies from pyproject.toml.
# To install dev/optional extras add them here, e.g.:
#   .[dev]
-e .

# Heavy optional bridges — uncomment as needed:
# discord.py
# matrix-nio
# matrix-nio[e2e]   # requires cmake + libolm

# Dev / test tools:
pytest
pytest-asyncio
