# secrets / local config
config.yaml
config.yaml.bak
.env
*temp*

# python
__pycache__/
.pytest_cache/
*.pyc
*.pyo
tinyctx.egg-info/
.docker-cache

# user custom modules
TinyCTX/custom_modules/

# sessions / workspace data
sessions/
workspace/

# tinyctx persistent data (bind-mounted into the containers)
# Keep the folder structure, ignore the databases/attachments themselves.
/data/**
!/data/
!/data/workspace/
!/data/config/
!/data/workspace/.gitkeep
!/data/config/.gitkeep

mcp.json