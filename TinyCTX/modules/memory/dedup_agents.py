"""
modules/memory/dedup_agents.py — SUPERSEDED in v2 by deduper.py.

Semantic deduplication now lives in deduper.py (embedding pass, candidate pairs
from the VectorIndex, greedy clique-edge-cover batching, LLM verification, and
the sqlite distinct-pair cache). Fuzzy-name near-duplicate detection is the
`fuzzy_names` flagger.

This stub remains only because file deletion is unavailable in this environment.
Do not import it.
"""
