The following entities are candidates for deduplication. Decide which (if any) refer to the same real-world thing and return merge operations as a JSON array. Return [] if all are distinct.

{entities}
