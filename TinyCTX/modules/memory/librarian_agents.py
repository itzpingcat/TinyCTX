"""
modules/memory/librarian_agents.py — SUPERSEDED in v2.

The librarian agents are now split by role: extractor.py (ingest), reviewer.py
(flagger-driven maintenance), deduper.py (semantic dedup). Shared plumbing
(agent loop, tool handler, sanitized conversation rendering) lives in
librarian_common.py.

This stub remains only because file deletion is unavailable in this environment.
Do not import it.
"""
